<h1> <%= current_user.username %>'s Paid Back Loans </h1>

<% @q.each do |completed| %>
    <%= User.find(completed.debtor_id).username %>
    <%= number_to_currency(completed.amt) %>
    <%= completed.description %> <br>
<% end %>